// App.tsx is intentionally minimal.
// Routing is handled by router.tsx (createBrowserRouter).
// The RouterProvider in main.tsx renders the router directly,
// so this file is a placeholder for future app-level providers
// or global components (e.g., toast notifications).

export default function App() {
  return null;
}
